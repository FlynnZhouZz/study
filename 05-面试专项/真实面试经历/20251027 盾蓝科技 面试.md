# 20251027 盾蓝科技 面试

公司名称: 深圳市盾蓝科技有限公司 <br>
面试岗位: React Native 开发工程师 <br>
薪资范围: 15-30K <br>
地址: 深圳市南山区讯美科技广场3号楼1613 <br>
面试时间: 2025/10/27 15:00 <br>

## 面试摘要

现在是离职还是在职？

上家公司离职原因？

自我介绍

看你有PHP讲师经历，做PHP讲师应该很厉害吧？
> 我当时的回答是，自我否定。本是想谦虚，现在听感觉，让人会感觉不自信、不厉害。

最近一份工作主要做什么？

React内置的一些Hooks和作用吗？

组件因为什么原因会重渲染？

有没有遇到组件内部状态或全局状态没有改变，组件也会重渲染？

全局状态管理Redux和Mobx有什么区别吗？实现原理分别是什么？优缺点？

根据demo来谈一谈

```js
const Child = ({ onClick }) => {
    console.log('child log');
    return (
        <div onClick={onClick}>text</div>
    );
}

const Parent = () => {
    console.log('parent log');
    const [current, setCurrent] = useState(0);

    useEffect(() => {
        for(let i = 1, i <= 100, i++) {
            setCurrent(i++);
        }
    }, []);

    const handleClick = () => {
        setCurrent(current++);
    }

    return (
        <div>
            <span>{current}</span>
            <Child onClick={handleClick} />
        </div>
    );
}
```

组件第一次进来，页面的`current`数字是多少？为什么？
在useEffect中，setCurrent只会执行第一次，react底层它是怎么做的？
我想让第一次进来，页面的`current`数字是，代码应该怎么改？
两个log打印的顺序？
当点击按钮的时候，我希望父组件打印log，子组件不打印log，代码如何更改？
使用了memo包住Child后，但是点击子组件还是会打印log？为什么？
为什么props改变，组件也会跟着重渲染？

类型体操？
常用的类型常用工具，如Pick等

前端以外的技术补充

之前实际的性能优化操作，常用的性能优化操作？
一个表格，某一行的某一列数据变动了，如何优化性能？

你有什么想问的，或你想了解我们的问题？
想了解一下项目情况？
是做区块链的，Web3，虚拟交易所

## 总结

回答话语不够简洁明了，答案没有点明关键点，模棱两可。
过程中太多的嗯、啊语气词，显得不够专业。
